# Installed pi patching skill

`SKILL.md` documents the workflow for changing skill discovery, activation, and prompt-loading behavior in the locally
installed pi package.

It records the relevant runtime files, verification approach, and current opt-in skill-loading semantics. Read
`SKILL.md` when making changes to pi's installed skill behavior.
